import { SiteConfig } from '../../types'

export const siteConfig: SiteConfig = {
  name: 'Folders',
  description:
    'A free web application for storing and sharing bookmark folders',
  url: 'localhost:3000',
}
